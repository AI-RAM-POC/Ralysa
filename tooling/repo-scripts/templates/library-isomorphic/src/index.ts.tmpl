// {{package}}: isomorphic library (loads in browsers and Node; no DOM, no Node built-ins).
export const PACKAGE_NAME = '{{package}}';

export function packageName(): string {
  return PACKAGE_NAME;
}
