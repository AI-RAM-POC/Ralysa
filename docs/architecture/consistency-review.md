# Architecture consistency review (pre-G3)

> Phase 3 · Owner: architect · Date: 2026-09-25 · Status: **Draft for G3 review**
> Scope: every document in `docs/architecture/` (overview, identity-and-policy, agent-protocol, model-gateway, mcp-gateway, workspace-runtime, data-model, observability-audit, deployment, security) and ADR-0001 to ADR-0029, written in parallel by streams A, B and C and the security reviewer. Cross-checked against the Phase 0 briefs `docs/features/F-00{1..5}-*/brief.md` (which the streams could not read), `docs/product/prd.md` (DV-1 to DV-19, NFRs) and `.claude/agents/architect.md`.
> Rules followed: every ADR stays **Proposed** (ADR-0023 is **Withdrawn**, merged into ADR-0019); no approval block is filled; no brief was edited; vendor facts and citations were kept, not added from memory.

## 1. How to use this document

- §2 lists every contradiction, duplicate and gap found, with the file-level resolution.
- §3 traces SR-01 to SR-32 (security.md §6) to where each is addressed, or marks it open.
- §4 is the table of **brief changes proposed at G4**. The briefs were not edited; the product owner decides each row at G4.
- §5 is the consolidated, de-duplicated open-questions list across all documents, with owners and a flag for the questions that block Phase 0 build.
- §6 lists what remains unresolved.

## 2. Issues found and resolutions

"Canonical" means the single place a rule is stated; other files now reference it.

| # | Issue | Files involved | Resolution (files changed) |
|---|---|---|---|
| C-01 | **Duplicate ADRs.** ADR-0019 (stream B, budget reservation ledger) and ADR-0023 (stream C, usage metering reserve/settle) record the same decision, with small differences: estimate formula (input × price + `max_tokens` × price vs `per_request_max_tokens` × price) and the Redis-loss rule (80 % snapshot rule vs conservative per-request cap). | adr/0019, adr/0023, observability-audit §7, §13, model-gateway §7, §14 | Kept **ADR-0019** (richer: five options, BYOM rule, reconciliation, Redis-loss rule). Folded in ADR-0023's unique content: options D (Postgres row locks) and E (post-hoc aggregation), Redis Lua reference, `PoolLedgerEntry` debit skipped for BYOM, `price_book_version`, per-request cap during rebuild (now combined with the 80 % rule), sandbox-seconds usage, "batch the ledger" revisit. ADR-0019's estimate formula is authoritative. **ADR-0023 status: "Withdrawn: merged into ADR-0019"** with a one-paragraph pointer. References updated in observability-audit §7 and §13, model-gateway §14, overview §7, adr/README. |
| C-02 | **Protocol source of truth.** ADR-0004: zod is the source of truth, JSON Schema generated. ADR-0012 and agent-protocol §2: JSON Schema 2020-12 is the source of truth. | adr/0004, adr/0012, agent-protocol §2, §4, overview §4 | One statement in ADR-0004 decision 5, repeated verbatim in ADR-0012 decision 1 and agent-protocol §2: **the published JSON Schema 2020-12 is the normative, language-neutral wire contract; it is authored in zod and generated from it; TypeScript types and validators come from the same zod source; CI fails if the committed JSON Schema and zod drift; zod features that don't export losslessly are banned in protocol schemas.** ADR titles adjusted. |
| C-03 | **Device-code sign-in.** ADR-0010 and identity-and-policy §3.1 had RTS host the device flow for every CLI sign-in. security.md TM-01 / SR-08 prefers the IdP-native flow so tenant Conditional Access applies, with loopback + PKCE as an alternative. F-002 AC-2 says "the control plane responds"; F-002 plans **no Ralysa-hosted web page** in Phase 0, which an RTS-hosted flow requires; F-005 AC-1 shows a code and URL. | adr/0010, identity-and-policy §2, §3, §3.1, §9, §13, overview §3, §4, security P0-3, OQ-S4 | ADR-0010 now compares three CLI flows on the same criteria and recommends: **(A) IdP-native device flow + one-time RFC 8693 exchange at RTS as the Phase 0 default** (meets SR-08, F-005 AC-1, and F-002's no-page constraint); **(B) loopback + PKCE (`/login --browser`)** always available and the default when a tenant disables device code (this is F-002 AC-1's browser-redirect grant); **(C) RTS-hosted device flow only as a Phase 1+ fallback** for IdPs without a device flow, carrying every SR-08 hardening item; plus a per-tenant switch that disables device code. The "IdP tokens never leave the control plane" rule is restated precisely (the CLI holds the IdP token in memory for one exchange; gateways never see IdP tokens). F-002 AC-2 wording needs a change (BC-01). |
| C-04 | **Policy engine vs decision model.** ADR-0002 recommends Cedar. ADR-0011 is engine-neutral and needs `deny` that a grant can lift, `mandatory_deny` that nothing lifts; in Cedar a `permit` can never override a `forbid`, so grants can't be `permit` policies. ADR-0002 didn't address this. Also ADR-0002 put distribution on "Redis pub/sub with ≤ 30 s poll", ADR-0011 on push + delta pull. | adr/0002, adr/0011, identity-and-policy §1, §2, §12 | ADR-0002 gains a criterion row ("a grant lifts a deny") and **Decision 2a**, a mapping table: `mandatory_deny` → grant-free `forbid`; `deny` → `forbid … unless { context.active_grants.contains(…) }`; grants are data in context, never `permit`; `require_approval` → `permit` + obligation; token/kill-switch/entitlement checked by the PEP before Cedar; determining policies → ADR-0011 reason codes; compiler tests. New risk and revisit trigger tie to ADR-0011's own trigger. Distribution now defers to ADR-0011 and the canonical fail-closed table. ADR-0011 cross-references ADR-0002 Decision 2a both in Related and in its engine-mapping section. |
| C-05 | **Kill-switch vs policy staleness.** ADR-0025: kill-switch state unconfirmed for 60 s → gateways refuse everything. ADR-0011 and identity §5.5: bundle unavailable for 5 min → refuse T2/T3 and writes. model-gateway §10: T1 continues ≤ 5 min. overview N12: "cache TTL ≤ 30 s backstop". overview N1: "a control-plane outage doesn't stop in-flight sessions until the token expires". These can't all be true. | adr/0011, adr/0025, identity-and-policy §5.5, model-gateway §10, observability-audit §9, overview N1, N12, adr/0019, adr/0022 | **Canonical table** added once: identity-and-policy.md **§5.6** (rules G-1 to G-8). G-1: governance heartbeat (kill-switch, epoch, revocations, latest policy version) by pub/sub + 5 s poll; unconfirmed > 60 s → refuse everything. G-2: bundle behind the announced version > 60 s → deny T2/T3 and writes; > 5 min *(proposed)* → deny everything (ADR-0011's single 5-minute budget was tightened so REQ-021b's 60 s holds). G-3 to G-8 cover PDP errors, session HWM store, audit intent, the local host, masking and budget counters. ADR-0011 item 5, ADR-0025, ADR-0019 decision 5, model-gateway §10, observability-audit §9 and overview N1/N12 now reference it. ADR-0025's obsolete revisit trigger ("move to bundles") updated: the kill-switch is in the bundle as a second copy; the heartbeat is authoritative. |
| C-06a | **Audit envelope.** Stream B (identity §9) assumed `event_id, event_type, ts, tenant_id, …, prev_hash`; stream C (observability §3) used `action, org_id, attested, on_behalf_of, decision_reason, …`; data-model and observability pointed at each other for the "full list". | observability-audit §3, identity-and-policy §9, model-gateway §12, mcp-gateway §9, agent-protocol §8, data-model §4 | **One canonical envelope in observability-audit.md §3.1**: `action` (not `event_type`), `org_id` (= stream B `tenant_id`), `actor.*` + `act` (replaces `on_behalf_of`), `source` + `attestation` (replaces `attested`), `reason_code` (replaces `decision_reason`), `endpoint_region` + `inference_region` + `inference_region_source` (SR-04), `client_seq`, `details` for event-specific fields. Chain fields (`shard`, `seq`, `prev_hash`) belong to AuditSeal, not the writer envelope. All other docs now reference §3.1. |
| C-06b | **Two-phase audit naming.** ADR-0022 and observability used `*.requested` / `*.completed`; model-gateway used `model.call.started` and `model.request.denied`; mcp-gateway used `tool.call.started`; the F-003/F-004 briefs use a single `tool.call` / `model.call` event and "exactly one audit event per call". | adr/0022, observability-audit §3.2, §4, model-gateway §2, §4, §10, §12, mcp-gateway §3, §4, §9, overview §3.1 | Canonical rule in observability-audit **§3.2**: `*.requested` (sync, before execution) → `*.completed` (outcome success/error/cancelled), or a single `*.denied`. Reconciliation: one terminal event per call and one intent per completion. model-gateway and mcp-gateway renamed; overview sequence diagram aligned; ADR-0022 states the rule and points to §3. Briefs: BC-06, BC-07. |
| C-06c | **Local-host audit (security TM-45(b)).** F-002 AC-11 accepts audit writes only from a service identity; F-003 AC-12 needs the local host, which holds only the user token (F-003 AC-2), to write tool audit; stream C marked such events `attested=false` without binding actor to token. | observability-audit §2, §3.3, §4, adr/0022, agent-protocol §7, §8, mcp-gateway §2, adr/0017, workspace-runtime §12, security P0-4 | **Two ingestion paths** (observability-audit **§3.3**), consistent with SR-29: the service path stays service-identity-only (F-002 AC-11 holds as written for services); a separate **client-attested path** accepts the user token, **overwrites `actor` and `org_id` from the token subject**, sets `attestation=client`, accepts only an allow-list of local-host events, requires a monotonic `client_seq` (gap detection), rate-limits, and refuses intents while a kill-switch covers the scope (TM-48). The intent ack carries governance state. Brief change BC-03. Server-mode hosts use the service path (workspace-runtime §12 corrected). |
| C-07 | **LiteLLM constraints.** ADR-0014 adopted LiteLLM without the security.md TM-37 / SR-26 constraints. | adr/0014, overview §3, security AS-5 | ADR-0014 now states the LiteLLM security history (with security.md's citations) and seven **release-gate constraints**: internal-only (NetworkPolicy, Ralysa authN in front), admin UI off, master key from Vault, **no provider credentials in LiteLLM's DB** (preferably no DB; per-request injection by the credential broker), no response/semantic caching and no external callbacks, **pinned image digest** from a verified release (never 1.82.7/1.82.8; ≥ 1.83.7), **72 h patch SLA**, SBOM/CVE gate. Fallback if a constraint can't be met: direct adapters (Option C) for the affected providers. New revisit trigger. |
| C-08 | **Phase 0 provider and region fields.** ADR-0005 (Vertex regional, europe-west1 candidate) agrees with F-004 OQ-F004-1. But: SR-04 wants `endpoint_region` and processing geography recorded separately, F-004 AC-9 and ADR-0005 recorded one `inference_region` from the endpoint config; model-gateway §4.3 used an example endpoint `vertex-me-central1`, which contradicts ADR-0005 (Google lists no Middle East region for Claude); F-004's rationale cites "Vertex Doha/Dammam" as the Phase 1 direction. | adr/0005, model-gateway §4.3, §5.1, §7.3, observability-audit §3.1, data-model §4, adr/0015 | Field model fixed once in the envelope: `endpoint_region` (configured), `inference_region` = **processing geography** (registry, with vendor evidence and date; the DV-12 name is kept), `inference_region_source`, and `inference_region_observed` where the provider reports it. ADR-0005 rule 3 records both (equal for a Vertex regional endpoint, which satisfies F-004 AC-9); rule 6 records `usage.inference_geo` for the Anthropic fallback. model-gateway example endpoint replaced with an illustrative in-country endpoint plus a note. ModelEndpoint and UsageRecord gain the fields. F-004 rationale: BC-08. |
| C-09 | **Brief cross-check.** The streams wrote without the briefs. | all briefs | See §4 (15 brief changes) and C-13 to C-22 for the architecture-side adjustments made because the briefs were right. |
| C-10 | **Agent Protocol additions requested by stream C.** A `trace` meta field on every JSON-RPC message (only notifications had `trace`), and a `governance.halted` notification (the protocol had `service.halted`). | agent-protocol §3.1, §3.4, §5.3, adr/0004 5a, observability-audit §8, §9, model-gateway §10 | `_meta` on **every** message (params / result / error.data, MCP convention) with `protocol` (negotiated version; also satisfies F-003 AC-8) and `trace` (W3C `traceparent`, optional `tracestate`); `_meta` never carries identity. `service.halted` renamed **`governance.halted`** (scope, scope_id, epoch, reason_category incl. `governance_stale`, i18n key) plus `governance.resumed`. |
| C-11 | **Unsupported-configuration risk.** overview, ADR-0006, ADR-0012, ADR-0014 and model-gateway described the risk and its mitigation differently (ADR-0004 vs ADR-0012 as the enabler; "Phase 2+" vs "if RA evals fail"); ADR-0015 didn't mention it. | overview §4.1, adr/0006, adr/0012, adr/0014, adr/0015, model-gateway §1, §9 | Named **ER-1** with one canonical statement in overview §4.1 and **one mitigation plan**, repeated verbatim: (1) REQ-030 eval gate per department through the real engine + translation path; (2) translation conformance tests in CI; (3) second-engine option: Phase 2 spike of an OpenAI-compatible EnginePort adapter with a **go/no-go before the RA pack enables any T3 department**, no client release needed. ADR-0015 now ties T3 availability to `eval_status` and ER-1. |
| C-12 | **Placeholder references** "ADR-0001…0007 (stream A)" and "per stream A ADR" in 9 documents and 3 ADRs; stale "brief not readable" notes (AQ-1, IQ-7, PQ-6, MQ-7, OQ-OA-8, OQ-WR-7). | workspace-runtime, mcp-gateway, data-model, deployment, identity-and-policy, observability-audit, agent-protocol, model-gateway, adr/0011, adr/0012, adr/0014 | Every placeholder now names the specific ADR (0001 language, 0002 engine, 0003 tenancy, 0004 transport, 0005 provider, 0006 runtime, 0007 search). Stale notes closed with pointers here. `security.md` line 4 still cites the old brief branch as its source; left as the security reviewer's historical source note. |
| C-13 | **Handshake, versioning and remote auth disagreed.** ADR-0004: `protocol.initialize`, semver, control-plane connect ticket. agent-protocol: `protocol.hello`, `MAJOR.MINOR`. identity §3.3: BFF cookie + Origin + CSRF. workspace-runtime: router validates an "SSO access token" (the browser holds none under BFF). | adr/0004, agent-protocol §3.2, §4, §7, workspace-runtime §2, §3, §12 | One design: `protocol.hello`; package semver with wire version `MAJOR.MINOR` in `_meta.protocol`; remote = BFF cookie + `Origin` check on upgrade + **single-use connect ticket in `protocol.hello`** (never in a URL), redeemed by the Session Router; sandbox host uses delegated tokens by token exchange. |
| C-14 | **Local transport.** agent-protocol §7 allowed a socket "bound to localhost" (a TCP listener); ADR-0004 and SR-15 forbid a listening TCP port. | agent-protocol §7, adr/0004 | stdio of the child process by default; otherwise Unix domain socket / named pipe with OS ACLs + per-launch secret. No TCP listener. Satisfies F-003 AC-7. |
| C-15 | **Where local stdio MCP servers get decisions.** mcp-gateway §2 and ADR-0017 said "the gateway's decision and audit API"; ADR-0011 item 6 and identity §6 said the Control Plane decision API. | mcp-gateway §2, adr/0017 | Control Plane decision API (ADR-0011) + client-attested audit path; the MCP Gateway doesn't see these calls. |
| C-16 | **Gateway overhead (N2) split** differed in overview (policy 10 ms, audit 15 ms, masking 50 ms), model-gateway §13 (PDP 5, audit 10, masking 60), ADR-0002 (≤ 10 ms entitlement + policy) and identity §10 (PDP ≤ 5 ms). | overview N2, model-gateway §13, adr/0002 | One split, owned by overview N2 *(proposed)*: authn 2 · entitlement + policy 5 · budget 5 · routing + credential 5 · audit intent 10 · masking ≤ 60 (≤ 8k incremental tokens) · headroom 13. MCP Gateway keeps its own split (mcp-gateway §10). |
| C-17 | **Availability claim.** overview N1 said a control-plane outage doesn't stop sessions until tokens expire; ADR-0025 and ADR-0022 deliberately stop calls after 60 s of stale governance state or when the audit DB is down. | overview N1 | N1 now states the coupling and that control plane, Redis and audit DB need gateway-level HA. |
| C-18 | **Missing protocol error.** F-005 AC-11 needs an "audit unavailable" category; the protocol had only `dependency_unavailable`. | agent-protocol §3.5, mcp-gateway §4.2, model-gateway §4.1, §10, adr/0022 | Added `−32012 audit_unavailable`; gateways return it when the intent commit fails. |
| C-19 | **When the audit hash chain starts.** security SR-29 / P0-4: hash chain from the first Phase 0 event. F-002 and observability §1: tamper-evidence in F-011 (Phase 1). | observability-audit §1, §4, adr/0021 | Architecture adopts SR-29: insert-only writer role and sealer chain from the first Phase 0 event; signed WORM checkpoints with F-011. Brief change BC-05. |
| C-20 | **Gap: Phase 0 server-side tool settings.** F-003 needs file-write/terminal on/off and redaction rules "server-side" before F-006 and asks at G3 where they live. No doc said. | identity-and-policy §5.4 | Per-organization settings document in the control-plane DB, served by `GET /v1/me/effective-permissions` in the `session.create` catalog; F-006 swaps in PDP decisions behind the same endpoint. |
| C-21 | **Gap: Phase 0 "T1 only" was procedural.** F-004 and F-005 say T1/synthetic only; security P0-6 asks for technical enforcement. | adr/0005 rule 5 | Deployment-level `max_tier = T1` enforced by the gateway, plus a CLI "synthetic data only" banner. Brief change BC-09. |
| C-22 | **Smaller mismatches.** data-model ModelEndpoint used `residency_class … foreign` vs ADR-0015 `locality … global`; identity §9 event names differed from F-002's (`auth.login.succeeded` vs `auth.sign_in`); SR-03, SR-05, SR-06, SR-07 items were missing from the topic docs; agent-protocol lacked SR-24 telemetry and SR-23 environment-scrubbing rows. | data-model §4, identity-and-policy §3.4, §4.3, §9, model-gateway §1, §6, agent-protocol §6.2 | ModelEndpoint aligned with ADR-0015 and SR-04. identity §9 **adopts the F-002 brief's names** (`auth.sign_in`, `auth.refresh`, `auth.sign_out`, `auth.token_rejected`, `audit.query`, `secret.rotated`). Added: SR-03 choke-point rule (model-gateway §1), SR-05 binary-media rule (model-gateway §6), SR-06 object-ID/tenant pinning (identity §3.4), SR-07 service-to-service identity (identity §4.3), SR-23/SR-24 rows (agent-protocol §6.2). |

## 3. SR traceability (security.md §6)

Status: **Addressed** = a doc or ADR states the control; **Partial** = the architecture states the principle, and named items are left to a design (G4) or test plan (G6); **Open** = not yet addressed in the architecture.

| SR | Requirement (short) | Where addressed | Status | What remains / owner |
|---|---|---|---|---|
| SR-01 | Server-side enforcement boundary; each control labelled server-enforced or client-assisted | overview §4.1 (+ residual risk); identity-and-policy §6; agent-protocol §7; ADR-0013; workspace-runtime §1 | Partial | Per-control classification for the Phase 0 briefs (P0-10): `/design F-003` |
| SR-02 | Session classification high-water mark, server-side | model-gateway §4.2; ADR-0015; mcp-gateway §4 stage 14; identity §5.6 G-4 | Addressed | Ownership confirmed as Redis store raised by MCP Gateway and Control Plane (answers OQ-S1); confirm at G3 |
| SR-03 | Single inference choke point; CI and network rule | model-gateway §1; ADR-0007 rule 3 (embeddings) | Addressed | CI rule implementation: F-001/F-004 design |
| SR-04 | Processing-geography registry; `endpoint_region` and processing geography in every record; global types denied for T2/T3 | observability-audit §3.1; model-gateway §5.1, §7.3; ADR-0005 rule 3; ADR-0015; data-model ModelEndpoint | Addressed | Quarterly re-verification is an ops runbook item |
| SR-05 | Gateway-side masking of the final payload; binary media; fail closed; per-session maps | model-gateway §6; identity §5.6 G-7 | Partial | Arabic detector choice and map storage details: F-008 design (CQ-12) |
| SR-06 | Groups by immutable ID; overage; issuer/tenant pinning; unmapped denied | identity-and-policy §3.4 | Addressed | Brief change BC-02 for F-002 AC-1 |
| SR-07 | Token model (≤ 15 min, one audience, rotation + reuse detection, keychain, s2s mTLS/workload identity, no passthrough, revocation ≤ 60 s) | identity-and-policy §4; ADR-0010; ADR-0016 | Addressed | DPoP optional (CQ-15) |
| SR-08 | Device-code hardening; IdP-native where possible; loopback + PKCE alternative | ADR-0010 (CLI sign-in section); identity-and-policy §3.1 | Addressed | IdP-native flow relies on IdP controls for code TTL and confirmation; security reviewer to confirm (§6 U-4) |
| SR-09 | Object-level authorization; RLS; IDOR matrix | identity §6 (PDP on every CP API); ADR-0003 (RLS); ADR-0011 | Partial | IDOR test matrix: G6 test plans |
| SR-10 | PDP safety: default deny, fail closed, policy tests, effective-permission diff, two-person approval for sensitive mappings | identity §1, §5.2, §5.6 (G-2, G-3); ADR-0002 (validation at save) | Partial | Effective-permission diff and two-person approval for T3 profile / admin / tier mappings: F-006 design |
| SR-11 | Server-issued approval grants verified by the executing gateway; step-up | ADR-0013; mcp-gateway §6 | Partial | Step-up mechanism (CQ-59) |
| SR-12 | Honest approval rendering (canonical payload, bidi, recipients, diff) | agent-protocol `approval.required` (exact payload + hash) | Partial | Rendering rules: F-010 / F-017 / F-021 design |
| SR-13 | Taint-aware gating; `web_fetch` off for T2/T3; egress proxy; calendar invites as external share | ADR-0018; mcp-gateway §5 | Partial | `web_fetch` default-off for T2/T3 and calendar classification: F-009 / F-010 design |
| SR-14 | Safe rendering; CSP; CLI escape sequences | mcp-gateway §5 item 5; ADR-0018 item 5 | Partial | CLI control/OSC escaping: brief change BC-10 (F-005); CSP/Trusted Types: F-015/F-016 |
| SR-15 | Client and origin hardening; local host on OS-protected IPC | ADR-0004 (no TCP port, OS-ACL IPC); agent-protocol §7; identity §3.3 (BFF, Origin) | Partial | Admin on separate origin or step-up; Electron hardening: F-015 / F-018 design |
| SR-16 | MCP registry integrity and residency | mcp-gateway §7; ADR-0017 | Addressed | — |
| SR-17 | Memory provenance | — | **Open** | No memory design yet: F-019 / F-030 design |
| SR-18 | Database-layer enforcement for connectors | mcp-gateway §4 stage 7, §8 (`dynamic_db_role`); ADR-0016 | Partial | Revoked functions, read-only roles, timeouts as a DB baseline: F-009 design |
| SR-19 | Agent-loop limits; kill-switch on every call | ADR-0025; identity §5.6 G-1; model-gateway §10 (rate limits) | Partial | Per-session max turns / tool calls / subagent depth: F-020 design |
| SR-20 | Cache and tenancy hygiene; no response caching | ADR-0003 (`org_id` on cache keys, RLS); ADR-0014 constraint 5 | Addressed | Vector ACL filters: ADR-0007 Phase 3 |
| SR-21 | Telemetry follows the tiers | ADR-0024; observability-audit §8 | Partial | Log-scrubbing library and crash-reporting rule: F-011 design |
| SR-22 | Isolation baseline (Phase 1 web host, Phase 2 sandbox) | ADR-0020; workspace-runtime §4 | Addressed | OQ-WR-5 (host and workbench in one pod for T3) |
| SR-23 | Credential brokering; scrubbed subprocess environments | ADR-0016; workspace-runtime §3, §7; agent-protocol §6.2 | Addressed | — |
| SR-24 | Local execution safety (pinned SDK config, OS sandbox, T2/T3 local tools only on managed devices) | agent-protocol §6.2 | Partial | OS-level sandbox for local terminal; managed-device rule (CQ-13); brief change BC-12 |
| SR-25 | Isolated extraction | overview §4 (extraction tagged untrusted) | **Open** | Phase 2, F-026 design |
| SR-26 | Secrets custody; no provider creds in any gateway DB incl. LiteLLM; non-exportable signing keys | ADR-0014 constraints 3–4; ADR-0016; model-gateway §8; ADR-0021 (KMS/Vault transit for checkpoints) | Partial | Non-exportable token-signing key for RTS: brief change BC-13; per-path Vault policies: deployment design |
| SR-27 | Plugin containment | — | **Open** | Phase 2 marketplace design; CQ-58 |
| SR-28 | Build and release integrity | ADR-0027 (cosign, SBOM, admission verification); ADR-0014 constraint 6 | Partial | CI hardening RF-1 to RF-5 in Phase 0: brief change BC-11 |
| SR-29 | Tamper-evident, complete audit; client-attested events with actor bound to token; sequence reconciliation | ADR-0021; ADR-0022; observability-audit §3.3, §4, §5 | Addressed, one deviation | Durable write-ahead queue not adopted (sync insert to HA audit DB instead); it is ADR-0022's revisit path (§6 U-3) |
| SR-30 | Connector identity binding; service-account connectors T1 by default | ADR-0016 (`service_account` flagged, end user asserted); mcp-gateway §8 | Partial | DWD impersonation subject from token, T1 default: F-027 design |
| SR-31 | Endpoint registration control; SSRF guard; region attestation | model-gateway §8 (keys bound to registered endpoints); ADR-0006 (local endpoints registered with deployment region) | Partial | SSRF guard on model-gateway egress, platform-admin-only registration: F-007 / F-030 design |
| SR-32 | Admin and vendor access hardening | ADR-0028 (licence sync metadata only); deployment §10 (support access customer-granted) | Partial | Phishing-resistant MFA for admins, step-up, admin-group alerts, SCIM cannot grant admin: F-006 / F-030 design |

## 4. Brief changes proposed at G4

The briefs were **not** edited. Each row is a proposal for the product owner at the brief's G4 (`/design F-nnn`). Where the brief was right, the architecture was changed instead (C-13 to C-22), and those cases are not repeated here.

| # | Brief / AC | Conflict | Proposed change | Architecture source |
|---|---|---|---|---|
| BC-01 | F-002 AC-2; F-005 AC-1 and Governance | AC-2 says "the control plane responds" to the device-authorization request; ADR-0010 now uses the IdP-native device flow in Phase 0 (SR-08) | F-002 AC-2: "The client starts a device-authorization sign-in **with the IdP**; after MFA, the client exchanges the IdP result at the control plane for Ralysa tokens; expired or reused codes are refused." Add: "A tenant setting disables device code; the CLI then uses `/login --browser` (AC-1 grant)." F-005 AC-1 unchanged in behaviour; add `--browser`. If the Entra tenant blocks device code (CQ-02), `--browser` becomes the default | ADR-0010; identity-and-policy §3.1 |
| BC-02 | F-002 AC-1, AC-5 | Groups are matched from "group claims"; no object-ID, tenant-pinning or overage rule (security TM-02, P0-1) | "Access and admin groups are configured and matched by immutable IdP object ID; issuer and tenant are pinned; if group overage can't be resolved, sign-in is denied and audited." | identity-and-policy §3.4; SR-06 |
| BC-03 | F-002 AC-11 | Only service identities can write audit, but F-003 AC-12 needs the local host (user token only) to write tool audit (TM-45(b)) | Add: "A second, client-attested endpoint accepts a user access token, only for local-host event types; it overwrites the actor from the token subject, marks events `attestation=client`, and refuses intents while a kill-switch applies." AC-11 stays as written for services | observability-audit §3.3; ADR-0022 |
| BC-04 | F-002 AC-11, AC-13; F-004 AC-4, AC-5 | One `inference_region` field; SR-04 needs endpoint region and processing geography separately | Add `endpoint_region` next to `inference_region` on AuditEvent and UsageRecord; define `inference_region` as processing geography | observability-audit §3.1; ADR-0005 rule 3 |
| BC-05 | F-002 Governance ("tamper-evidence … F-011") | SR-29 / P0-4: an insert-only writer role and a hash chain from the first event, because a later chain can't vouch for earlier events | Add AC: "The audit writer role has INSERT only; UPDATE/DELETE are rejected and audited; each event is sealed into a hash chain within 5 s." Signed WORM checkpoints, verify and retention stay in F-011 | ADR-0021; observability-audit §1, §4 |
| BC-06 | F-003 AC-12, Governance; F-005 Governance | "An audit event … Tool-call count equals audit-event count"; two-phase audit writes an intent before and a completion after | "Each tool call has a `tool.call.requested` intent acknowledged before execution and a `tool.call.completed` (or one `tool.call.denied`), paired by `tool_call_id`; every call has exactly one terminal event." Rename `tool.call` accordingly | observability-audit §3.2; ADR-0022 |
| BC-07 | F-004 AC-4, Governance; F-005 AC-6 | "Exactly one audit event per call" | "Every call has exactly one terminal event (`model.call.completed` or `model.call.denied`), and each completed call has exactly one `model.call.requested` written before the provider call." Rename `model.call` accordingly | observability-audit §3.2; ADR-0022 |
| BC-08 | F-004 OQ-F004-1 (rationale) | Cites "Phase 1 in-region direction (Vertex Doha/Dammam)"; Google lists no Middle East region for Claude on Vertex (ADR-0005, MA-302) | Keep the recommendation (Vertex regional); replace the rationale with "deterministic region for AC-9 and DV-12; Doha/Dammam availability for Claude is unverified (PRD OQ-5)" | ADR-0005 |
| BC-09 | F-004 Governance; F-005 Governance | "T1 only" is procedural; security P0-6 asks for technical enforcement | Add F-004 AC: "The Phase 0 gateway runs with `max_tier = T1` and denies any request whose effective tier is higher." Add F-005 AC: "The CLI shows a 'synthetic data only' banner in Phase 0 environments" | ADR-0005 rule 5 |
| BC-10 | F-005 (no AC) | The CLI is the first renderer of untrusted model and tool output; terminal control and OSC sequences are not stripped (TM-50, P0-8, SR-14) | Add AC: "C0/C1 control characters and OSC sequences in model and tool output are escaped before rendering; a test set of ANSI/OSC-52/OSC-8 payloads renders inert" | security.md TM-50, SR-14 |
| BC-11 | F-001 AC-2 and Out of scope ("dependency scanning and SBOM are REQ-098, Phase 2") | security PG-1 / P0-9 / SR-28 and repository findings RF-1 to RF-5 need SHA-pinned actions, a `permissions` block, a lifecycle-script allow-list, SCA and a release environment with human approval in Phase 0 | Add ACs for RF-1 to RF-5 to F-001 (at least SHA pins, least-privilege `permissions`, install-script allow-list, SCA gate, protected release environment); SBOM and signing can follow with ADR-0027 in Phase 1. Product owner to decide with PRD PG-1 | security.md §5.8, §8.4 PG-1; ADR-0027 |
| BC-12 | F-003 AC-1, AC-2 | SDK configuration and subprocess environment are not asserted (TM-31, TM-33, P0-7) | Add AC: "The engine adapter runs with no filesystem setting sources, an explicit tool allow-list, the Model Gateway as base URL, non-essential traffic off, never bypass mode; tool subprocesses inherit no credentials" | agent-protocol §6.2; ADR-0012 |
| BC-13 | F-002 AC-9, AC-10 | Token-signing keys "held in the vault"; SR-26 / P0-2 want non-exportable signing keys (KMS/HSM or Vault transit) with JWKS | "Token-signing keys are non-exportable and used through a signing API; services verify through JWKS" | identity-and-policy §4; SR-26 |
| BC-14 | F-004 Governance (`gateway.config_changed`) | Architecture event name is `model.registry.changed` (model-gateway §12) | Rename to `model.registry.changed` (or keep both as aliases in Phase 0). Low priority | model-gateway §12 |
| BC-15 | F-003 AC-7 | "Connection from another machine" presumes a network endpoint; the host uses stdio of a child process by default | Clarify: "The host exposes no network listener. When a local IPC channel is used (Desktop), a connection without the per-launch secret or from another OS user is refused and logged" | ADR-0004 decision 2; agent-protocol §7 |

## 5. Consolidated open questions

De-duplicated across overview (AQ), identity-and-policy (IQ), agent-protocol (PQ), model-gateway (MQ), mcp-gateway (GQ), observability-audit (OQ-OA), data-model (OQ-DM), deployment (OQ-DP), workspace-runtime (OQ-WR), security (OQ-S) and the architecture-relevant brief questions (OQ-F00x). Closed in this review and not repeated: AQ-1, IQ-7, PQ-6, MQ-7, OQ-OA-8, OQ-WR-7 (briefs unreadable); OQ-S2 (LiteLLM vs custom: ADR-0014 with the TM-37 constraints, pending G3, tracked as CQ-00); OQ-S1 (HWM owner: ADR-0015); OQ-F003-3 (vendor-neutral schema: yes, ADR-0004).

**P0** = blocks Phase 0 build (design or development of F-001 to F-005).

| # | Question | Merged from | Recommendation | Owner | P0 |
|---|---|---|---|---|---|
| CQ-00 | G3 acceptance of the ADRs that Phase 0 designs depend on: 0001 (language), 0004 (protocol), 0005 (provider), 0010 (token service, CLI flows), 0012 (engine port), 0014 (Model Gateway + LiteLLM constraints), 0021 and 0022 (audit) | F-002/F-003/F-004 "Architecture (G3)" dependencies; OQ-S2 | Review and decide at G3 | Tech lead (G3 approver) | **Yes** |
| CQ-01 | Anthropic commercial terms for embedding the Agent SDK in a resold, customer-operated product and reselling tokens | AQ-2(a), PQ-3, PRD OQ-4, OQ-F003-1 | Obtain in writing; F-003 development doesn't start without it (brief) | Founder | **Yes** |
| CQ-02 | Does the Phase 0 Entra test tenant (and the pilot tenant) allow the device-code flow under Conditional Access? | IQ-3, OQ-S4 | Check before `/design F-002`. If blocked, loopback PKCE is the Phase 0 default and BC-01 changes F-005 AC-1 | Architect + pilot IT | **Yes** |
| CQ-03 | Phase 0 Vertex set-up: is the chosen Claude model listed on the `europe-west1` regional endpoint (or another region) with enough quota; which exact model version is pinned? | ADR-0005 rule 1–2, OQ-F004-1, OQ-F004-2 | Confirm at F-004 set-up; fall back to the Anthropic API (`us`) per ADR-0005 rule 6 | Architect + product owner | **Yes** |
| CQ-04 | Pull the insert-only role and hash chain into Phase 0 (BC-05)? | SR-29, P0-4; F-002 Governance | Yes; checkpoints stay in F-011 | Product owner + security reviewer | **Yes** |
| CQ-05 | Accept the audit brief changes (client-attested path BC-03; two-phase reconciliation BC-06, BC-07; region fields BC-04)? Without them F-002 AC-11, F-003 AC-12, F-004 AC-4/AC-9 and F-005 AC-6 can't pass as written | TM-45(b); C-06 | Accept at G4 | Product owner | **Yes** |
| CQ-06 | Vault product for Phase 0 dev and the reference install (HashiCorp Vault vs cloud secret manager) | F-002 and F-004 external dependencies; spec §8 | HashiCorp Vault for dev and on-prem (the design uses its database secrets engine and transit signing); cloud KMS/secret managers behind the same broker interface | Tech lead | **Yes** |
| CQ-07 | Pull CI hardening RF-1 to RF-5 into F-001 (BC-11)? | PG-1, P0-9, SR-28 | Yes for SHA pins, permissions, install-script allow-list, SCA, protected release environment | Product owner + tech lead | **Yes** |
| CQ-08 | Local host process model: child process over stdio for CLI and Desktop? | PQ-1; F-005 "how the CLI launches the host" | Yes (default in ADR-0004 and agent-protocol §7); confirm at `/design F-003` | Architect + developer | No (default set) |
| CQ-09 | Access- and refresh-token lifetimes | IQ-1, OQ-F002-2 | 15 min access; 12 h idle / 7 d absolute refresh; T3 8 h absolute | Security reviewer + pilot CISO | No (defaults set) |
| CQ-10 | Governance thresholds: G-1 60 s, G-2 60 s / 5 min; per-tier relaxation of G-1 for T1? | ADR-0011 item 5, ADR-0025 revisit | Accept as proposed; no T1 relaxation until pilot data | Architect + CISO advisor | No |
| CQ-11 | ER-1: second-engine go/no-go for T3 local models | MQ-1, AQ-2(b) | Spike in Phase 2; decide before the RA pack enables any T3 department | Architect + product owner | No |
| CQ-12 | Arabic + English PII detector within the N2 masking budget; where it runs | AQ-4; model-gateway "Future" ADR | Spike in F-008 design; golden set per REQ-029 | Architect + solution designer | No |
| CQ-13 | T3 and local tools on Desktop/CLI: allowed at all? Managed-device requirement via Conditional Access? | AQ-5, OQ-S3 | No local tools or stdio MCP for T3; T3 chat only through gateways | Product owner + CISO advisor | No |
| CQ-14 | Can masked T2 text go to a foreign-hosted model (QCB Art. 21.4)? | AQ-3, MQ-3, OQ-S5, PRD OQ-2 | Default T2 to local or in-country for bank tenants until counsel answers | Founder + legal | No |
| CQ-15 | Verified in-country inference endpoints for Qatar/KSA; can the serving region be observed per call? | MQ-2, MQ-6, PRD OQ-5 | Assume none; `global` endpoints T1-only | Product owner + architect | No |
| CQ-16 | Provider contract terms per model deployment: no training, retention | OQ-S14, security AS-8 | Record in the model registry next to processing geography | Product owner | No |
| CQ-17 | Arabic model shortlist and GPU sizing for the bundled runtime | AQ-6, OQ-DP-8, PRD OQ-6 | Size after REQ-030 evals | Architect + product owner | No |
| CQ-18 | Retention modes per tier, also applied to traces | OQ-DM-1, OQ-S7, PRD OQ-16 | Accept PRD OQ-16 defaults; extend to traces. Phase 0 is metadata-only | Product owner + CISO advisor | No |
| CQ-19 | DPoP for CLI/Desktop refresh tokens | IQ-2 | Phase 4 unless the pilot asks | Architect | No |
| CQ-20 | Manager resolution for approvals | IQ-4, PRD OQ-18 | IdP `manager` attribute with admin override; confirm SCIM exposure | Product owner + architect | No |
| CQ-21 | Grant maximum durations per tier | IQ-5 | T1 90 d, T2 30 d, T3 7 d | Product owner | No |
| CQ-22 | Volume of `policy.decision` allow events | IQ-6 | Full for T2/T3; denies + sampled allows for T1 | Architect + CISO advisor | No |
| CQ-23 | Support window for the previous protocol major | PQ-2 | 6 months | Product owner | No |
| CQ-24 | Session resume across host / SDK versions | PQ-4 | Within the same major host version | Architect | No |
| CQ-25 | Attachments out-of-band vs inline | PQ-5 | Out-of-band, size limit per REQ-004b | Solution designer | No |
| CQ-26 | Substitute-on-tier vs deny-and-ask when the HWM rises | MQ-4 | Tenant option, default deny-and-ask | Product owner | No |
| CQ-27 | Credit price for local-model usage | MQ-5 | 0 credits; meter tokens and GPU-seconds | Founder | No |
| CQ-28 | DB sources: pooled `SET ROLE` vs per-user dynamic credentials | GQ-1 | Offer both; default dynamic per-role credentials | Architect + pilot DBA | No |
| CQ-29 | Taint rule for drafts in T3 sessions | GQ-2 | Approval for T3 drafts only | Product owner + security reviewer | No |
| CQ-30 | Pilot ServiceNow/Jira per-user OAuth support | GQ-3, PRD A-7 | Per-user OAuth | Pilot discovery | No |
| CQ-31 | Support window for MCP 2025-11-25 upstream servers | GQ-4 | ≥ 12 months, reviewed quarterly | Architect | No |
| CQ-32 | Injection-scanner model (local, Arabic + English) | GQ-5 | Local classifier + rules, evaluated on the REQ-063 set | Security reviewer | No |
| CQ-33 | Approval grant lifetime vs scheduled agents | GQ-6 | 10 min interactive; runbooks for scheduled agents | Product owner | No |
| CQ-34 | Checkpoint interval acceptable to bank regulators (tamper window) | OQ-OA-1 | 60 s default, 10 s for regulated tenants | CISO advisor | No |
| CQ-35 | Block the next call in a T3 session while a completion write is spooled? | OQ-OA-2 | Yes for T3 | Architect | No |
| CQ-36 | Tail-sampling rate and trace retention | OQ-OA-3 | 10 %, 14 days; confirm at pilot | Ops + architect | No |
| CQ-37 | Langfuse in the Phase 1 bundle | OQ-OA-4 | Grafana-only in Phase 1; Langfuse with F-032 | Architect | No |
| CQ-38 | Two-person rule to deactivate a tenant-wide kill-switch | OQ-OA-5 | Yes | Product owner | No |
| CQ-39 | Device-attested local audit | OQ-OA-6 | Phase 4 hardening; unattested until then | Architect | No |
| CQ-40 | Pilot SIEM and whether a SIEM feed is needed at pilot | OQ-OA-7, security PG-4 | Ask at pilot discovery; offer syslog/HTTP at pilot if required | Product owner + pilot | No |
| CQ-41 | UsageRecord detail retention | OQ-DM-2 | 25 months detail, 7 years aggregates | Pilot finance + product owner | No |
| CQ-42 | Pseudonymising deprovisioned users in audit | OQ-DM-3 | Pseudonymous id + separately retained identity map; counsel to confirm | Counsel | No |
| CQ-43 | Source of national classification labels | OQ-DM-4 | Manual per ConnectorBinding in Phase 1; labels in Phase 4 | Architect | No |
| CQ-44 | Workspace files in the exit bundle | OQ-DM-5 | Opt-in second job | Product owner | No |
| CQ-45 | Separate audit database for Phase 4 SaaS | OQ-DM-6 | Decide in the Phase 4 SaaS ADR | Architect | No |
| CQ-46 | Pilot cloud (Azure Qatar Central, Google Doha, on-prem) | OQ-DP-1, PRD OQ-1 | Azure and on-prem Terraform first unless the pilot says otherwise | Founder + pilot | No |
| CQ-47 | Managed Postgres with zone redundancy and PITR per target region | OQ-DP-2 | Verify per region; CloudNativePG fallback | Architect | No |
| CQ-48 | Cross-provider in-country DR acceptable to QCB/NCSA | OQ-DP-3 | Ask the pilot's risk team | Pilot risk + founder | No |
| CQ-49 | RPO 24 h for web workspace files | OQ-DP-4 | Accept for Phase 2; hourly option | Product owner | No |
| CQ-50 | Counsel review of the classification mapping and NCSA level names | OQ-DP-5, OQ-S6 | Before the first regulated pilot | Counsel + product owner | No |
| CQ-51 | Zarf vs in-house air-gap bundle | OQ-DP-6 | Spike in Phase 3 | Architect | No |
| CQ-52 | Partner-managed variant in Phase 1 | OQ-DP-7 | Customer-operated only in Phase 1 | Founder | No |
| CQ-53 | Definitions of web cold start and resume | OQ-WR-1 | Accept workspace-runtime §6 | Product owner | No |
| CQ-54 | Warm-pool sizing from the pilot's peak claim rate | OQ-WR-2 | Start at 10 % of Web seats | Architect | No |
| CQ-55 | Block-volume attach latency per target cloud | OQ-WR-3 | Benchmark in Phase 1 | Architect | No |
| CQ-56 | Egress proxy TLS interception | OQ-WR-4 | SNI/FQDN only by default | Security reviewer | No |
| CQ-57 | Host and workbench in one sandbox for T3 workspaces | OQ-WR-5 | Security reviewer to rate | Security reviewer | No |
| CQ-58 | Plugin UI panel isolation | OQ-S9 | Decide before the marketplace design | Architect | No |
| CQ-59 | Step-up authentication for approvals and admin actions | OQ-S10 | Entra authentication context for the pilot; IdP-agnostic design | Architect | No |
| CQ-60 | Web-search tool in scope and who provides it | OQ-S11 | No provider-side server tools for T2/T3 | Product owner | No |
| CQ-61 | Licence sync contents and vendor support access | OQ-S12 | Answered by ADR-0028 (licence id and seat counts only) and deployment §10; founder to confirm for the QCB annex | Founder | No |
| CQ-62 | Control IDs for NIA, NCA ECC/CCC, SAMA CSF | OQ-S13 | CISO advisor against primary texts | CISO advisor | No |
| CQ-63 | WORM / object-lock availability per target, incl. sovereign partner clouds | OQ-S8 | Answered per hyperscaler in deployment §7; verify partner clouds | Architect | No |
| CQ-64 | Kata/gVisor node-pool availability in-region | OQ-WR-6 | Verify before F-023/F-024 | Architect | No |

## 6. Remaining unresolved conflicts and risks

| # | Item | Why it is still open | Who resolves |
|---|---|---|---|
| U-1 | Brief vs architecture on audit counting, ingestion, region fields, device flow and T1 enforcement (BC-01 to BC-09) | Briefs may not be edited by agents; until G4 accepts the changes, those ACs contradict the architecture | Product owner at G4 (CQ-05) |
| U-2 | ER-1: non-Claude models through the Claude harness are vendor-unsupported | A vendor-support risk, not a document conflict; mitigation plan is consistent everywhere | Architect + product owner (CQ-11) |
| U-3 | SR-29 asks for fail-closed "through a durable queue"; ADR-0022 uses a synchronous insert into an HA audit DB | Accepted deviation for Phase 0–1 (simpler, testable); the queue is ADR-0022's revisit path | Security reviewer to accept or reject at G3 |
| U-4 | SR-08 items "code TTL ≤ 10 min" and "confirmation page with client/IP/geo" can't be enforced by Ralysa in the IdP-native flow (ADR-0010 flow A); they rely on the IdP's controls and Conditional Access. IP-mismatch alerting depends on what sign-in data the IdP exposes | The recommendation meets SR-08's "IdP-native where possible" but not every hardening item in that mode | Security reviewer at G3 |
| U-5 | G-2 thresholds changed from ADR-0011's first draft (5 min → 60 s for T2/T3 and writes, new 5 min all-deny) | Consistency needed it; stream B and the tech lead haven't confirmed the numbers | Tech lead at G3 (CQ-10) |
| U-6 | Availability coupling: a > 60 s control-plane or Redis outage stops all model and tool calls (G-1), and the audit DB is on the call path | Deliberate governance-first trade-off; may be rejected by the pilot for T1 | Product owner + pilot (CQ-10) |
| U-7 | security.md PG-1 (move REQ-098 supply-chain parts to Phase 1) vs F-001 and the PRD (Phase 2) | Product phasing decision | Product owner (CQ-07) |
| U-8 | Partial SRs (§3): SR-17, SR-25 and SR-27 are open; 18 SRs are partial (11 addressed) and need design-time closure | Feature-level detail belongs to the solution designer; tracked here so G4 reviews check them | Solution designer + security reviewer at each G4 |

## Approval (G3)

> Filled by a human only. Agents must leave this blank.

| Approver | Role | Decision (Approved / Changes requested) | Date | Notes |
|---|---|---|---|---|
| | | | | |
